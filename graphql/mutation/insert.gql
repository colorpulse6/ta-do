const ADD_CATEGORY = gql `
    mutation ($book_string: String!) {
    insert_books(objects: {title: $book_string}) {
      affected_rows
      returning {
        id
        title
        description
        created_at
      }
    }
  }
  `;